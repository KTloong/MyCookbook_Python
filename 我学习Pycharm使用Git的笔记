首先确保电脑已经安装了anaconda和pycharm 安装好了python等等需要的东西

google 搜索 git download，在官网下载git windows版本（64位）
下载好后安装git

安装好后打开pycharm，file-》setting version control-》git-》test test自动检测成功后点OK

接着点击VCM，选择git
登录GIthub账号，然后选择需要的repo的url，然后选择本地存放位置

右键 git（提交文件） 右键commit file（对修改进行陈述） 添加commite信息 选择commite and push， 点击push

————————以上内容是如何将Github上的东西同步到本地以及本地做了修改怎么同步到Github——————————
那么如何将Github cloud end 被修改的东西拉到本地呢？这就需要一下操作！

首先VCS-》Git-》pull 点击 pull 即可


--------------------创建了new branch如何在Git Master中检测到呢？ 答案是需要按照如下操作进行！

首先点击PyCharm中的Git Master查看是否有相应的Branch
接着点击PyCharm下面的Terminal 输入命令 git fetch
执行命令后便可以对Git Master中的Branch进行刷新了，然后点击新的Branch点击checkout输入Branch在本地的名字，接着就可以对新
的Branch进行修改了！

——————————pull和pull request是两个概念需要区分，pull是将Github上面的东西拉到本地，pull request（merge）
是一个repo到另一个repo的合并过程。比如：把自己的new branch中的东西PR到masterbranch中。这个操作应该这么做：

这个只需要在github中点击new pull request即可，一步一步来就能完成操作！不难O(∩_∩)O
https://youtu.be/MBdLOl5tUKY 这个视频简明地讲了pull和PR的区别以及使用方法，值得学习！收获很大！

——————————点了commit忘记push怎么办！
这个问题曾经困扰了我，但是今天发现挺简单。首先点击VCS，then点击Git 然后点击 push，然后就会发现要push的内容了！